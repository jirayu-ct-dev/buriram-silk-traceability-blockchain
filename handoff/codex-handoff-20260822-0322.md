# Handoff

- วันที่: 2026-08-22 03:22
- Repo: `/Users/jirayu/dev/github/buriram-silk-traceability-blockchain` (branch `dev`)
- สถานะ: งาน wire API จริง + normalize ตัวหนังสือไทย เสร็จแล้ว gates ทัง 4 ผ่าน — **ยังไม่ commit**

## Next-session focus

- ทบทวน diff ทังหมด (30 ไฟล์แก้ไข + 8 path ใหม่) แล้วตัดสินใจ commit/PR ตามที่ผ้้ใช้สั่ง
- ถ้ามุ่่งตอ: ทำ phase ถัดไปตาม `docs/system-design.md` §19 (build order) และอัปเดต `WORK_PROGRESS_REPORT.md`

## Objective

- เป็็น mock data/`setTimeout` ทิงให้เหลือเพีียงการ reset copy feedback — เป็็น `$fetch` ทังหมด
- สร้าง API endpoints ทิงให้หน้า dashboard ใช้งานได้
- Normalize ตัวหนังสือไทยทิงให้ตรงกกันทังโปรเจกต (canonical = `app/pages/weaver/items/new.vue`)

## Current state

### งานใน working tree (uncommitted)

1. **Wire API จริง (frontend)** — แก้ mock เป็็น `$fetch`:
   - `app/pages/transfers/index.vue` (ใหญ่สุด: +157 บรรทัด — สร้างการส่่งมอบด้วย select silk item/organization จริง)
   - `app/pages/transfers/[id].vue`, `app/pages/review/index.vue`, `app/pages/review/[id].vue`
   - `app/pages/ledger/index.vue`, `app/pages/ledger/[height].vue`
   - `app/pages/certificate/[id].vue`, `app/pages/certificates/[id]/manage.vue`, `app/pages/audit/[id].vue`
   - `app/pages/weaver/index.vue`, `app/pages/weaver/items/[id].vue`, `app/pages/weaver/items/new.vue`, `app/pages/login.vue`
2. **API endpoints ใหม่ (untracked)**:
   - `server/api/audit/[silkItemId].get.ts`
   - `server/api/ledger/verify.get.ts`, `server/api/ledger/blocks/index.get.ts`
   - `server/api/organizations/index.get.ts`
   - `server/api/reviews/[id].get.ts`
   - `server/api/transfers/[id].get.ts`
   - `server/api/certificates/[id].get.ts`
   - `server/api/silk-items/[id]/revisions.post.ts`
3. **หน้า edit ใหม่**: `app/pages/weaver/items/[id]/edit.vue` (untracked)
4. **Types/session**:
   - `shared/types/api.ts` — เพิ่ม `SessionUser.organizationId`, `OrganizationOption`, `SilkItemSummary.certificateStatus`, `PublicCertResult` + `CHAIN_INVALID`
   - `app/plugins/auth.ts` — expose `organizationId`
   - `app/composables/useStatusMeta.ts` — เพิ่ม status `CHAIN_INVALID`
5. **Normalize ตัวหนังสือไทย** — แก้ ~43 จุดใน `weaver/items/[id]/edit.vue` และอีก ~10 ไฟล์ (audit, certificate, ledger, review, transfers, login ฯลฯ)

### สิ่งที่ยังไม่ทำ

- Commit ใด ๆ (ผ้้ใช้ยังไม่สั่ง)
- Phase ถัดไปของ build order ใน `docs/system-design.md`

## Decisions and constraints

- กฎเหล็กจาก `AGENTS.md`: gates `pnpm typecheck && pnpm lint && pnpm test && pnpm test:e2e` ต้องผ่านก่อนส่งมอบ; ห้าม commit/push เอง; UI ภาษาไทย; ใช้ semantic color token (`bg-primary`) ห้าม hard-code palette; ใช้ `useToast()`/`useConfirm()` ห้าม `alert()`; zod เท่านั้น ห้าม `any`
- Source of truth: `docs/system-design.md` (routes §5, workflow §6, ledger §8, build order §19) และ `docs/database-design.md`
- Canonical ตัวหนังสือไทย: `app/pages/weaver/items/new.vue` — ไฟล์อื่่นต้องตรงกกัน
- Ports: app 3007, postgres host 5437 (ห้าม 3000/5432); ใน docker network ใช้ `postgres:5432`
- ห้ามแตะ: `.env`, `storage/`, private key, `app/generated/`

## Relevant artifacts

- `AGENTS.md` — กฎการทำงาน + คำสั่ง
- `WORK_PROGRESS_REPORT.md` — สถานะ Phase 1 ✅ / Phase 2 ✅ (mock ครบ 17 หน้า) — ตอนนี้กำลังผ่าน "wire API จริง"
- `docs/system-design.md`, `docs/database-design.md` — design source of truth
- `test/e2e/frontend.spec.ts`, `test/e2e/home.spec.ts`, `test/unit/silk-item-new.spec.ts` — specs ทิงผ่านแล้ว

## Verification performed

- `pnpm typecheck` → EXIT:0
- `pnpm lint` → 0 errors, 145 warnings, EXIT:0
- `pnpm test` → 3 files / 14 tests passed, EXIT:0
- `pnpm test:e2e` → 24 tests passed (42s), EXIT:0
- ตรวจแล้วว่าไม่มี mock data ทิงใน `app/pages` (setTimeout ที่เหลือเพีียง reset copy feedback)

## Blockers and risks

- **งานทังหมดยัง uncommitted** — เสี่่ยงหายถ้ามื้ือ; ควร commit ทันทีที่ผ้้ใช้ยอม
- Lint warnings 145 รายการ (pre-existing, 0 errors) — ไม่ขวาง gates แต่ควรเคลียร์ทีหลัง
- ตัวหนังสือไทยใน tool output อาจแสดงผลเพี้ยน — ตอนแก้ไข string ให้ใช้ Python `pathlib` replace + ตรวจ byte-level อย่า trust visual
- `test-results/` directory ใหม่จาก e2e run (gitignored อยู่แล้ว)

## Recommended next steps

1. `git diff` ทังหมดเพื่อบรีฟผ้้ใช้/ทบทวนคุณภาพ (หรือใช้ skill `scrutinize`)
2. Commit แยกตาม logical unit ถ้ามื้ือสั่ง: (a) wire API + endpoints ใหม่, (b) normalize ตัวหนังสือไทย, (c) edit.vue
3. อัปเดต `WORK_PROGRESS_REPORT.md` ให้สะท้อนสถานะ wire API จริง
4. ตอ phase ถัดไปตาม `docs/system-design.md` §19

## Suggested skills

- `web-ui-coding-standards` — ต้องโหลดก่อนแก้ไข/สร้าง UI ทุกครังตามกฎเหล็กใน AGENTS.md
- `scrutinize` — ใช้ review diff ใหญ่ชุดนี้ก่อน commit
- `prisma-client-api` — ใช้ตอนเขีย้่น query ใน endpoints ใหม่/แก้ไข
- `handoff` — ใช้สร้าง handoff ฉบับใหม่เมื่อจบ session ถัดไป
